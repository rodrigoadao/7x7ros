izlude,60,167,4	script	Gest√£o de cartas	651,{
	//  if (checkweight(1201,1) == 0) {
	//  	mes "^3355FFgordo^000000";
	//  	close;
	//  }

	//inicio do npc
	switch(select("Comprar cartas de MVP:Remover cartas:Devolver cartas de MvP:Resetar zenys")) {
	//case de comprar cartas de mvp
	case 1:
		mes "Os tokens intransfer√≠veis que v√™m na cria√ß√£o do seu personagem podem ser trocados por cartas MvPs. Escolha cautelosamente, pois voc√™ n√£o receber√° novos tokens no seu personagem.";
		switch(select("Bijou, 10 Totens:Cutie, 1 Toten:Immortal Cursed Knight, 1 Toten:Immortal Wind Ghost, 1 Toten:Beelzebub, 5 Totens:TGK, 10 Totens:Eremes, 10 Totens:Maya Purple, 10 Totens:Kiel, 5 Totens")) {
		case 1:
			delitem 7831,10;
			getitem 4636,1;
			break;
		case 2:
			delitem 7831,1;
			getitem 27152,1;
			break;
		case 3:
			delitem 7831,1;
			getitem 31024,1;
			break;
		case 4:
			delitem 7831,1;
			getitem 31025,1;
			break;
		case 5:
			delitem 7831,5;
			getitem 4145,1;
			break;
		case 6:
			delitem 7831,10;
			getitem 4302,1;
			break;
		case 7:
			delitem 7831,10;
			getitem 4359,1;
			break;
		case 8:
			delitem 7831,10;
			getitem 4198,1;
			break;
		case 9:
			delitem 7831,5;
			getitem 4403,1;
			break;
	}
	next;
	mes "Fa√ßa o favor de jogar direito. Busque o teto da performance.";
	close;
	end;

	//case de remover cartas - REFATORADO COM MAPEAMENTO CORRETO
case 2:
	mes "[Removedor de Cartas]";
	mes "Qual equipamento deseja remover cartas?";

	deletearray .@slot_map;
	set .@menu$, "";
	set .@menu_count, 0;

	// Ordem correta: Head Top, Head Mid, Armor, Left Hand, Right Hand, Garment, Shoes, Accessory L
	
	// 6 = Head Top (equip slot 1)
	if( getequipisequiped(1) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 1;
		set .@menu$, .@menu$ + getequipname(1) + ":";
	}

	// 5 = Head Mid (equip slot 9)
	if( getequipisequiped(9) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 9;
		set .@menu$, .@menu$ + getequipname(9) + ":";
	}

	// 7 = Armor (equip slot 2)
	if( getequipisequiped(2) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 2;
		set .@menu$, .@menu$ + getequipname(2) + ":";
	}

	// 9 = Left Hand (equip slot 3)
	if( getequipisequiped(3) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 3;
		set .@menu$, .@menu$ + getequipname(3) + ":";
	}

	// 8 = Right Hand (equip slot 4)
	if( getequipisequiped(4) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 4;
		set .@menu$, .@menu$ + getequipname(4) + ":";
	}

	// 3 = Garment (equip slot 5)
	if( getequipisequiped(5) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 5;
		set .@menu$, .@menu$ + getequipname(5) + ":";
	}

	// 2 = Shoes (equip slot 6)
	if( getequipisequiped(6) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 6;
		set .@menu$, .@menu$ + getequipname(6) + ":";
	}

	// 1 = Accessory L (equip slot 7)
	if( getequipisequiped(7) ) {
		set .@menu_count, .@menu_count + 1;
		set .@slot_map[.@menu_count], 7;
		set .@menu$, .@menu$ + getequipname(7) + ":";
	}

	if (.@menu_count == 0) {
		mes "VocÍ n„o possui equipamentos equipados.";
		close;
	}

	next;
	set .@selected, select(.@menu$);
	
	if (.@selected > .@menu_count || .@selected == 0) {
		mes "SeleÁ„o inv·lida.";
		close;
	}

	set .@part, .@slot_map[.@selected];

	if(.@part == 0) {
		mes "Erro ao selecionar equipamento.";
		close;
	}

	if(getequipcardcnt(.@part) == 0) {
		mes "Este equipamento n„o possui cartas.";
		close;
	}

	set .@cardcount, getequipcardcnt(.@part);

	if (!checkweight(1202, (.@cardcount + 1))) {
		mes "Seu invent·rio est· muito cheio!";
		close;
	}

	next;
	successremovecards .@part;
	next;
	mes "Sucesso! Cartas removidas.";
	close;
	end;


		//inicio do case devolver cartas mvp
		case 3:
			
			switch(select("Fechar:Devolver cartas de MvP")) {
				
				case 1:
					close;

				case 2:
					mes "Qual carta MvP voc√™ quer devolver?";
					switch(select("Bijou, 10 Totens:Cutie, 1 Toten:Immortal Cursed Knight, 1 Toten:Immortal Wind Ghost, 1 Toten:Beelzebub, 5 Totens:TGK, 10 Totens:Eremes, 10 Totens:Maya Purple, 10 Totens:Kiel, 5 Totens")) {
					case 1:
						delitem 4636,1;
						getitem 7831,10;
						break;
					case 2:
						delitem 27152,1;
						getitem 7831,1;
						break;
					case 3:
						delitem 31024,1;
						getitem 7831,1;
						break;
					case 4:
						delitem 31025,1;
						getitem 7831,1;
						break;
					case 5:
						delitem 4145,1;
						getitem 7831,5;
						break;
					case 6:
						delitem 4302,1;
						getitem 7831,10;
						break;
					case 7:
						delitem 4359,1;
						getitem 7831,10;
						break;
					case 8:
						delitem 4198,1;
						getitem 7831,10;
						break;
					case 9:
						delitem 4403,1;
						getitem 7831,5;
						break;
				}
				break;
			}
		next;
		mes "Fa√ßa o favor de jogar direito. Busque o teto da performance.";
		close;
		end;

		case 4:
			set Zeny, 20000000;
			break;
		
			next;
		}
		next;
		mes "Fa√ßa o favor de jogar direito. Busque o teto da performance.";
		close;
		end;

	next;
	mes "Fa√ßa o favor de jogar direito. Busque o teto da performance.";
	close;
}
